<form method="post" action="{{ path('app_ability_delete', {'id': ability.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ability.id) }}">
    <button class="btn">Delete</button>
</form>
